@model IEnumerable<CellPhoneConnection.NhaSanXuat>

<li class="dropdown">
    <a href="@Url.Action("Index","SanPham")" class="dropdown-toggle" data-toggle="dropdown">Smart phone</a>
    <div class="dropdown-menu">

            <ul class="list-unstyled">
                @foreach (var item in Model)
                {
                    <li><a href="@Url.Action("Index","NhaSanXuat", new { id = @item.a_IDNhaSX})">@item.a_TenNhaSX</a></li>

                }
            </ul>

    </div>
</li>